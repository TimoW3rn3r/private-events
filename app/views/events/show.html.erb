<h2><%= @event.name %></h2>

<h4>Creator</h4>
<%= @event.creator.fullname %>

<h4>Location</h4>
<%= @event.location %>

<h4>Date</h4>
<%= @event.date %>

<h4>Time</h4>
<%= @event.time %>

<h4>Attendees</h4>
<% @event.attendees.each do |attendee| %>
  <%= attendee.fullname %>
<% end %>
